server:
  port: 8086
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pessoa
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true